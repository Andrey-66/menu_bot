---
#@generated
#docker run --rm -e RUN_LOCAL=true -e VALIDATE_PYTHON=true -v D:/Projects/Menu_bot:/tmp/lint -v D:/Projects/Menu_bot/config/lint:/tmp/lint/config/lint github/super-linter
name: Lint Code Base
on:
  push: null
  pull_request: null
jobs:
  build:
    name: Lint
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: read
      statuses: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Super-linter
        uses: super-linter/super-linter@v6.1.1
        env:
          VALIDATE_CHECKOV: false
          VALIDATE_PYTHON_PYLINT: false
          VALIDATE_PYTHON: true
          LINTER_RULES_PATH: ./config/lint
          PYTHON_BLACK_CONFIG_FILE: pyproject.toml
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}